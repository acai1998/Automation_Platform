# ========================================
# Docker Swarm 环境配置
# 非敏感环境变量 - 敏感信息使用 Docker Secrets
# ========================================

# 应用基础配置
NODE_ENV=production
PORT=3000

# 数据库配置 (非敏感)
DB_HOST=117.72.182.23
DB_PORT=3306
DB_USER=root
DB_NAME=autotest

# Jenkins 配置 (非敏感)
JENKINS_URL=http://jenkins.wiac.xyz:8080
JENKINS_USER=root
JENKINS_JOB_NAME=AutoTest
JENKINS_JOB_API=SeleniumBaseCi-AutoTest
JENKINS_JOB_UI=ui-automation
JENKINS_JOB_PERF=performance-automation

# JWT 配置 (非敏感)
JWT_EXPIRES_IN=7d

# 网络配置
CORS_ORIGIN=http://localhost
API_CALLBACK_URL=http://localhost:3000/api/jenkins/callback
JENKINS_ALLOWED_IPS=localhost,127.0.0.1,jenkins.wiac.xyz
JENKINS_DEBUG_IP=false

# ========================================
# 敏感配置说明
# ========================================
# 以下配置已迁移到 Docker Secrets，不再在此文件中配置：
#
# DB_PASSWORD          -> Docker Secret: db_password
# JENKINS_TOKEN        -> Docker Secret: jenkins_token
# JENKINS_API_KEY      -> Docker Secret: jenkins_api_key
# JENKINS_JWT_SECRET   -> Docker Secret: jenkins_jwt_secret
# JENKINS_SIGNATURE_SECRET -> Docker Secret: jenkins_signature_secret
# JWT_SECRET           -> Docker Secret: jwt_secret
#
# 应用会自动从 /run/secrets/ 目录读取这些敏感配置
# ========================================